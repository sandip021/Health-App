import 'package:flutter/material.dart';

class CalorieDataScreen extends StatelessWidget {
  const CalorieDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Calorie Data Screen'));
  }
}