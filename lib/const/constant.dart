import 'package:flutter/material.dart';

const cardBackgroundColor = Color(0xFF1B2A22); // Dark greenish color for cards
const primaryColor = Color(0xFF4CAF50); // Primary mid-tone green
const secondaryColor = Color(0xFFA5D6A7); // Lighter green for secondary elements
const backgroundColor = Color.fromARGB(255, 136, 193, 188); // Brighter greenish background
const selectionColor = Color(0xFF81C784); // A fresh green shade for selection

const defaultPadding = 20.0;
